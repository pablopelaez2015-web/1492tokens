# Copia este archivo como .env en /opt/1492tokens/ y ajusta los valores
# que te devuelva deploy/detect-traefik.sh
TRAEFIK_NETWORK=root_default
TRAEFIK_ENTRYPOINT=websecure
TRAEFIK_CERTRESOLVER=letsencrypt
